//---------------------------------------------------------------------------

#pragma hdrstop
//---------------------------------------------------------------------------
// Include
//---------------------------------------------------------------------------

#include "UnitRootFinder.h"
//---------------------------------------------------------------------------

//#include <vcl.h>
namespace NSApplication {
namespace NSKernel {
/*
class CRootFinderTester {
public:
  CRootFinderTester()
    : Text(L"RootFinder Test:\n") {
    CSpeedApproximation ApproximationT;
    CSpeedApproximation Approximation;
    std::vector<double> Data;
    Data.push_back(400.0);
    CInterval Interval(350.0, 450.0);
    ApproximationT.setApproximation(Data);
    Approximation.setApproximation(Data);
    Text += L"Root = ";
    Text += CNewtonRootFinderT::findRoot<1>(ApproximationT, Interval);
    Text += L"\nRoot = ";
    Text += CHalleyRootFinderT::findRoot<1>(ApproximationT, Interval);
    Text += L"\nRoot = ";
    Text += CSchroederRootFinderT::findRoot<1>(ApproximationT, Interval);
    Text += L"\nRoot = ";
    Text += CRootFinder::findRoot1(Approximation, Interval);


    ShowMessage(Text.c_str());
  }
private:
  UnicodeString Text;
};
CRootFinderTester RootFinderTester;*/
//---------------------------------------------------------------------------
} // NSKernel
//---------------------------------------------------------------------------
} // NSApplication
//---------------------------------------------------------------------------
#pragma package(smart_init)
